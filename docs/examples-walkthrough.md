# Examples Walkthrough

The starter demo in `nano-agent-stack` shows a CEO-style intent routed into a research department with manager and worker roles. The templates repository extends this with common operational patterns.

